conky.config = {
    own_window = true,
    own_window_type = 'normal',
    own_window_hints = 'undecorated,sticky,skip_taskbar,skip_pager,above',

    own_window_transparent = true,
    own_window_argb_visual = true,
    own_window_argb_value = 0,

    double_buffer = true,

    alignment = 'top_right',
    gap_x = 8,
    gap_y = 5,

    minimum_width = 50,
    minimum_height = 20,

    draw_shades = false,
    draw_outline = false,
    draw_borders = false,

    use_xft = true,
    font = 'Iosevka:size=9',

    default_color = '#E8D8D2',
    update_interval = 10,
};

conky.text = [[
${execi 30 ~/.local/bin/battery}
]];
